apiVersion: v1
kind: ConfigMap
metadata:
  name: fcg-grafana-datasources
data:
  datasource.yml: |
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        access: proxy
        url: http://fcg-prometheus:9090
        isDefault: true
        uid: prometheus
        editable: false
