apiVersion: v1
kind: ConfigMap
metadata:
  name: fcg-grafana-dashboards-provider
data:
  dashboards.yml: |
    apiVersion: 1
    providers:
      - name: FCG
        orgId: 1
        folder: FCG
        type: file
        options:
          path: /var/lib/grafana/dashboards
